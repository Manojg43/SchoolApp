name: Render Keep Alive

on:
  schedule:
    - cron: "*/14 * * * *" # Run every 14 minutes
  workflow_dispatch: # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -I https://schoolapp-6vwg.onrender.com/api/
